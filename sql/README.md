# SQL 表结构说明

这个目录用于存放当前项目中各个 Supabase 数据表的结构解读文档。

当前包含：

- `app_users.md`
- `audio_records.md`
- `user_progress.md`
- `user_mistakes.md`

说明原则：

1. 文档内容优先贴合当前项目真实用法
2. 字段含义基于现有前端代码中的读写逻辑整理
3. 如某些字段是“代码中明确使用”的，文档会直接说明
4. 如某些字段是“当前建议设计”，文档会明确标注

